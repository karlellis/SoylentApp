import React from "react";
import ReactDOM from "react-dom/client";
import Main from "./App.jsx";

function App() {
  return <h1>Migration in progress</h1>;
}

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);